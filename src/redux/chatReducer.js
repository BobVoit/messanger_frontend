
// initialState
let initialState = {
    messages: [],
}



const chatReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}



export default chatReducer;